panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 2,
label_size = 3,
legend.size = 7,
layout.exp = 1.5
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.29)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat")) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 2,
label_size = 3,
legend.size = 7,
layout.exp = 2
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.29)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat")) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 2,
label_size = 3,
legend.size = 6,
layout.exp = 2
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.29)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat")) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 2,
label_size = 3,
legend.size = 2,
layout.exp = 2
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.29)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat")) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 2,
label_size = 3,
legend.size = 7,
layout.exp = 2
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.29)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat")) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 2,
label_size = 3,
legend.size = 7,
layout.exp = 2
) +
geom_text(size= 9)+
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.29)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat")) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 2,
label_size = 3,
legend.size = 7,
layout.exp = 2,
size = 5
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.29)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat")) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 2,
label_size = 3,
legend.size = 7,
layout.exp = 2,
size = 2
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.29)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat")) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 2,
label_size = 3,
legend.size = 7,
layout.exp = 2,
size = 3
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.29)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat")) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 2,
label_size = 3,
legend.size = 7,
layout.exp = 2,
size = 3 #argument of geom_text for the diagonals
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.29)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat")) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 1,
label_size = 3,
legend.size = 7,
layout.exp = 2,
size = 3 #argument of geom_text for the diagonals
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.29)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
?fig.align
ggcorr_custom(subset(redwines, select= c("quality.cat","quality","sulphates","alcohol", "volatile.acidity")) ) + ggplot2::theme_light(base_size= 8)
multi1 <- ggplot(aes(x = sulphates, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi2 <- ggplot(aes(x = sulphates, y = volatile.acidity, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi3 <- ggplot(aes(x = volatile.acidity, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
grid.arrange(multi1,multi2,multi3, ncol=1) + theme_light(base_size = 10)#+ ggplot2::guides(color= FALSE, fill=FALSE);
ggcorr_custom(subset(redwines, select= c("sulphates","alcohol", "volatile.acidity", "quality.cat","quality")) ) + ggplot2::theme_light(base_size= 8)
multi1 <- ggplot(aes(x = sulphates, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi2 <- ggplot(aes(x = sulphates, y = volatile.acidity, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi3 <- ggplot(aes(x = volatile.acidity, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
grid.arrange(multi1,multi2,multi3, ncol=1) + theme_light(base_size = 10)#+ ggplot2::guides(color= FALSE, fill=FALSE);
ggcorr_custom(subset(redwines, select= c("sulphates","pH","total.sulfur.dioxide", "quality.cat","quality")) ) + ggplot2::theme_light(base_size= 8)
multi1 <- ggplot(aes(x = pH, y = sulphates, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi2 <- ggplot(aes(x = pH, y = total.sulfur.dioxide, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi3 <- ggplot(aes(x = sulphates, y = fixed.acidity, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
grid.newpage()
grid.arrange(multi1,multi2,multi3, ncol=1) + theme_light(base_size = 10);
ggcorr_custom(subset(redwines, select= c("total.sulfur.dioxide","pH", "sulphates", "quality.cat","quality")) ) + ggplot2::theme_light(base_size= 8)
multi1 <- ggplot(aes(x = pH, y = sulphates, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi2 <- ggplot(aes(x = pH, y = total.sulfur.dioxide, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi3 <- ggplot(aes(x = sulphates, y = fixed.acidity, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
grid.newpage()
grid.arrange(multi1,multi2,multi3, ncol=1) + theme_light(base_size = 10);
corr_total_1 <- ggcorr_custom(subset(redwines, quality.cat=="bad"), layot_exp = 2) + ggplot2::theme_light(base_size= 8) + ggplot2:: ggtitle("Bad")
corr_total_2 <- ggcorr_custom(subset(redwines, quality.cat=="medium"), layot_exp = 2) + ggplot2::theme_light(base_size= 8) + ggplot2:: ggtitle("Medium")
corr_total_3 <- ggcorr_custom(subset(redwines, quality.cat=="good"), layot_exp = 2) + ggplot2::theme_light(base_size= 8)  + ggplot2:: ggtitle("Good")
gridExtra::grid.arrange(corr_total_1, corr_total_2, corr_total_3, ncol = 3, name = "Correlation vs Quality") + ggplot2:: theme_light(base_size = 6)
#temp <- gridExtra::arrangeGrob(corr_total_1, corr_total_2, corr_total_3, nrow = 3, name = "Correlation vs Quality") + ggplot2:: theme_light(base_size = 10)
#grid.newpage()
#grid.draw(temp)
ggcorr_custom(subset(redwines, select= c("volatile.acidity", "alcohol", "sulphates", "quality.cat","quality")) ) + ggplot2::theme_light(base_size= 8)
multi1 <- ggplot(aes(x = sulphates, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi2 <- ggplot(aes(x = sulphates, y = volatile.acidity, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi3 <- ggplot(aes(x = volatile.acidity, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
grid.arrange(multi1,multi2,multi3, ncol=1) + theme_light(base_size = 10)#+ ggplot2::guides(color= FALSE, fill=FALSE);
unlink('P4-Analysis_of_a_dataset_cache', recursive = TRUE)
ggcorr_custom(subset(redwines, select= c("volatile.acidity", "alcohol", "sulphates", "quality.cat","quality")) ) + ggplot2::theme_light(base_size= 8)
multi1 <- ggplot(aes(x = sulphates, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi2 <- ggplot(aes(x = sulphates, y = volatile.acidity, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi3 <- ggplot(aes(x = volatile.acidity, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
grid.arrange(multi1,multi2,multi3, ncol=1) + theme_light(base_size = 10)#+ ggplot2::guides(color= FALSE, fill=FALSE);
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat"), layot_exp = 0) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 1,
label_size = 3,
legend.size = 7,
layout.exp = layot_exp,
size = 3 #argument of geom_text for the diagonals
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.28)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines) + ggplot2::theme_light(base_size= 7)
ggcorr_custom <- function(x, method = "pairwise", palette = "RdYlGn", cols_to_avoid = c("X", "quality.cat"), layot_exp = 0) {
require(ggplot2)
require(GGally)
require(grid)
require(gridBase)
require(gridExtra)
column_names = colnames(x)
cols_names = column_names[!(column_names %in% cols_to_avoid)]
po.nopanel <- list(theme(
panel.background = element_blank(),
panel.grid.minor = element_blank(),
panel.grid.major = element_blank(),
axis.text.x = element_text(angle = -90)))
corr <-ggcorr(data=subset(x, select = cols_names),
label = TRUE,
geom = "blank",
hjust = 1,
angle = -25,
digits = 2,
nbreaks = 8,
name = "",
label_round = 1,
label_size = 3,
legend.size = 7,
layout.exp = layot_exp,
size = 3 #argument of geom_text for the diagonals
) +
geom_point(aes(size=abs(coefficient), color = breaks,
alpha = round((abs(coefficient) > 0.28)*abs(coefficient), 1))) +
scale_color_brewer(palette = "RdBu")+
scale_alpha(range=c(0.,.7))+
scale_size(range=c(0,9))+
guides(fill= FALSE, color= FALSE, size = FALSE, alpha = FALSE)
return(corr)
}
ggcorr_custom(redwines, layot_exp = 2) + ggplot2::theme_light(base_size= 7)
corr_total_1 <- ggcorr_custom(subset(redwines, quality.cat=="bad"), layot_exp = 2) + ggplot2::theme_light(base_size= 8) + ggplot2:: ggtitle("Bad")
corr_total_2 <- ggcorr_custom(subset(redwines, quality.cat=="medium"), layot_exp = 2) + ggplot2::theme_light(base_size= 8) + ggplot2:: ggtitle("Medium")
corr_total_3 <- ggcorr_custom(subset(redwines, quality.cat=="good"), layot_exp = 2) + ggplot2::theme_light(base_size= 8)  + ggplot2:: ggtitle("Good")
gridExtra::grid.arrange(corr_total_1, corr_total_2, corr_total_3, ncol = 3, name = "Correlation vs Quality") + ggplot2:: theme_light(base_size = 6)
#temp <- gridExtra::arrangeGrob(corr_total_1, corr_total_2, corr_total_3, nrow = 3, name = "Correlation vs Quality") + ggplot2:: theme_light(base_size = 10)
#grid.newpage()
#grid.draw(temp)
ggcorr_custom(subset(redwines, select= c("volatile.acidity", "alcohol", "sulphates", "quality.cat","quality")) ) + ggplot2::theme_light(base_size= 8)
multi1 <- ggplot(aes(x = sulphates, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi2 <- ggplot(aes(x = sulphates, y = volatile.acidity, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi3 <- ggplot(aes(x = volatile.acidity, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
grid.arrange(multi1,multi2,multi3, ncol=1) + theme_light(base_size = 10)#+ ggplot2::guides(color= FALSE, fill=FALSE);
ggcorr_custom(subset(redwines, select= c("volatile.acidity", "alcohol", "sulphates", "quality.cat","quality")), layot_exp = 1 ) + ggplot2::theme_light(base_size= 8)
multi1 <- ggplot(aes(x = sulphates, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi2 <- ggplot(aes(x = sulphates, y = volatile.acidity, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
multi3 <- ggplot(aes(x = volatile.acidity, y = alcohol, fill= quality.cat), data = redwines)+ geom_point(alpha=1/5, aes(color= quality.cat))+ facet_wrap(~redwines$quality.cat) +theme_light(base_size = 8)
grid.arrange(multi1,multi2,multi3, ncol=1) + theme_light(base_size = 10)#+ ggplot2::guides(color= FALSE, fill=FALSE);
